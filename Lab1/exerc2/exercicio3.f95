program strings
	implicit none
 
	character(len=30) :: nome  ! string chamada nome com 30 caracteres de comprimento
	character(len=64) :: frase ! string chamada frase com 64 caracteres de comprimento

end program
